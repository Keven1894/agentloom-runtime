# OSS release scanner allowlist
#
# One substring per line. Any flagged line that CONTAINS one of these substrings
# is downgraded to "allowlisted" and will not fail the gate.
#
# Use this ONLY for confirmed false positives or intentionally-public strings.
# Do NOT use it to silence real internal info — fix the source instead.
#
# Review every addition: an over-broad token here defeats the gate.

# --- Examples (uncomment / adapt per release) ---------------------------------
# example@example.com          # placeholder email used in docs
# 10.0.0.0                     # documented CIDR example, not a real host

# --- Confirmed-OK public strings ---------------------------------------------
# Author attribution in the LICENSE copyright line (intentional, public):
Copyright (c) 2025-2026 Dr. Boyuan (Keven) Guan, FIU Library & GIS Center
bguan@fiu.edu

# --- Session memory (Layer 0) documentation examples --------------------------
# SCP-style Git remotes look like email addresses to the scanner. These are
# fictional example repositories used to show workspace-key normalization.
Acme/widget
acme/widget
acme/Widget.git
acme/a.git
acme/b.git
# Generic cross-platform path illustration, not a real developer checkout:
`C:\projects\widget` on Windows and `/home/dev/widget` on Linux
``C:\\projects\\widget`` becomes ``c-projects-widget``
workspace_slug(Path("C:/projects/widget"))

# --- Transcript redaction test fixtures ---------------------------------------
# Synthetic credential-shaped strings that exist *because* the tests assert they
# get redacted. None is a real key; each is a literal in tests/test_transcript.py.
sk-abcdefghijklmnopqrstuvwxyz123456
sk-abcdefghijklmnopqrstuvwxyz01
ghp_aBcDeFgHiJkLmNoPqRsTuVwXyZ012345
xoxb-1234567890-abcdefghij
AKIAIOSFODNN7EXAMPLE
mysql://admin:sup3rSecret@db.example.org
